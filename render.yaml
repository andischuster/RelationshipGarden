services:
  - type: web
    name: relationship-garden
    env: node
    plan: free
    buildCommand: ls -laR && npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: / 